syntax = "proto3";

package logs;

option go_package = "/auth";

message AuthRequest{
  string token = 1;
}

message AuthResponse {
  bool isAuth = 1;
}

service AuthService {
  rpc CheckAuth(AuthRequest) returns (AuthResponse);
}